/**
 * @brief This file contains miscilanious scene pieces
 */
 
include <DND Play Area Config.scad>;
use <DND Play Area Tiles.scad>;
use <DND Play Area Circles>;

/**
 * @brief Creates the arch for the doorway
 * @param _length the length of the arch in tile units
 * @param _archHeight Height of the arch in tile units
 */
 module doorway_arch(_length,_archHeight) {
     
 }
 